import { css } from 'docz-plugin-css'

export default {
  title: 'Knight',
  description: 'A Design UI library for _So_',
  codeSandbox: false,
  plugins: [
    css({
      preprocessor: 'postcss'
    })
  ]
}

